# Deployment S³

S³ is optimized for straightforward deployment to Node or Serverless environments.

There are no persistent, custom long-running daemons. The compiler translates the S³ DSL into standard runtime JavaScript, ensuring compatibility with platforms like Railway, Render, Fly.io, or VPS, but highly prioritizing the Vercel Build Output API.
